name: Ansible Deploy
on: 
    workflow_dispatch:
        inputs:
          Server:
             type: choice
             description: 'Server: '
             required: true
             options:
             - vCenter
             - ESXi
             - NSX-T
jobs:    
    build:
      runs-on: [self-hosted]
      steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
       
      - if: github.event.inputs.Server == 'vCenter'
        name: Run Ansible to deploy vCenter
        run: >
            ansible-playbook --extra-vars "VCENTER_TOKEN=${{ secrets.VCENTER_TOKEN }}"
            --extra-vars "NESTED_VC_TOKEN=${{ secrets.NESTED_VC_TOKEN }}"
            Ansible/vCenter/run.yml
